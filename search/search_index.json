{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"FastPG","text":"<p>FastPG is a lightweight async ORM layer for PostgreSQL applications, especially FastAPI services. It uses <code>pydantic</code> models for schema validation and the <code>databases</code> package for async I/O, while keeping the generated SQL straightforward.</p>"},{"location":"#what-it-includes","title":"What It Includes","text":"<ul> <li><code>DatabaseModel</code> for model definitions and instance-level <code>save()</code> / <code>delete()</code></li> <li><code>AsyncQuerySet</code> for lazy read and write queries</li> <li><code>Relation</code> and <code>Prefetch</code> for explicit relationship loading</li> <li><code>AsyncRawQuery</code> for hand-written SQL with FastPG error wrapping</li> <li><code>AsyncPaginator</code> and <code>RawQueryAsyncPaginator</code></li> <li><code>Transaction</code> helpers for the configured write connection</li> </ul>"},{"location":"#important-implementation-notes","title":"Important Implementation Notes","text":"<ul> <li>FastPG requires at least one <code>READ</code> connection and exactly one <code>WRITE</code> connection.</li> <li>Single-row <code>create()</code> only performs <code>INSERT ... RETURNING</code>; conflict handling exists on <code>bulk_create()</code>, not on <code>create()</code>.</li> <li><code>update()</code> and <code>delete()</code> should always be chained after <code>filter(...)</code>. The safe pattern is required by the current implementation.</li> <li><code>select_related()</code> uses one relation at a time and only the first supplied relation name is used.</li> </ul>"},{"location":"#minimal-example","title":"Minimal Example","text":"<pre><code>from fastpg import DatabaseModel\n\n\nclass User(DatabaseModel):\n    id: int | None = None\n    email: str\n\n    class Meta:\n        db_table = \"users\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n\n\nasync def list_users():\n    return await User.async_queryset.all()\n</code></pre>"},{"location":"#read-next","title":"Read Next","text":"<ul> <li><code>getting-started.md</code></li> <li><code>guides/models.md</code></li> <li><code>guides/querysets.md</code></li> <li><code>guides/relationships.md</code></li> <li><code>guides/pagination.md</code></li> <li><code>guides/transactions.md</code></li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#1-install","title":"1. Install","text":"<p>From a local checkout:</p> <pre><code>pip install -e .\n</code></pre> <p>From GitHub:</p> <pre><code>pip install git+https://github.com/bepragma-ai/fastpg.git\n</code></pre> <p>FastPG targets Python <code>&gt;=3.8</code>.</p>"},{"location":"getting-started/#2-configure-fastpg","title":"2. Configure FastPG","text":"<p>Create one FastPG instance during app startup:</p> <pre><code>import os\nfrom fastapi import FastAPI\nfrom fastpg import ConnectionType, create_fastpg\n\napp = FastAPI()\n\nFAST_PG = create_fastpg(\n    name=\"api\",\n    databases={\n        \"default\": {\n            \"TYPE\": ConnectionType.WRITE,\n            \"USER\": os.environ[\"POSTGRES_WRITE_USER\"],\n            \"PASSWORD\": os.environ[\"POSTGRES_WRITE_PASSWORD\"],\n            \"DB\": os.environ[\"POSTGRES_WRITE_DB\"],\n            \"HOST\": os.environ[\"POSTGRES_WRITE_HOST\"],\n            \"PORT\": os.environ[\"POSTGRES_WRITE_PORT\"],\n        },\n        \"replica_1\": {\n            \"TYPE\": ConnectionType.READ,\n            \"USER\": os.environ[\"POSTGRES_READ_USER\"],\n            \"PASSWORD\": os.environ[\"POSTGRES_READ_PASSWORD\"],\n            \"DB\": os.environ[\"POSTGRES_READ_DB\"],\n            \"HOST\": os.environ[\"POSTGRES_READ_HOST\"],\n            \"PORT\": os.environ[\"POSTGRES_READ_PORT\"],\n        },\n    },\n    tz_name=\"UTC\",\n    query_logger={\n        \"LOG_QUERIES\": True,\n        \"TITLE\": \"MY_SERVICE\",\n    },\n)\n</code></pre> <p>Connection rules:</p> <ul> <li>At least one <code>ConnectionType.READ</code> connection is required.</li> <li>Exactly one <code>ConnectionType.WRITE</code> connection is expected.</li> <li>Reads go through a randomly chosen read connection.</li> <li>Writes always use the configured write connection.</li> </ul> <p>If you register more than one FastPG instance, switch the active one with <code>set_current_fastpg(name)</code>.</p>"},{"location":"getting-started/#3-open-and-close-connections","title":"3. Open and Close Connections","text":"<pre><code>@app.on_event(\"startup\")\nasync def on_startup():\n    await FAST_PG.db_conn_manager.connect_all()\n\n\n@app.on_event(\"shutdown\")\nasync def on_shutdown():\n    await FAST_PG.db_conn_manager.close_all()\n</code></pre>"},{"location":"getting-started/#4-define-a-model","title":"4. Define a Model","text":"<pre><code>from datetime import datetime\nfrom fastpg import DatabaseModel\n\n\nclass Customer(DatabaseModel):\n    id: int | None = None\n    name: str\n    email: str\n    created_at: datetime | None = None\n\n    class Meta:\n        db_table = \"customers\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n        auto_now_add_fields = [\"created_at\"]\n</code></pre>"},{"location":"getting-started/#5-run-crud-queries","title":"5. Run CRUD Queries","text":"<pre><code># Create\nnew_customer = await Customer.async_queryset.create(\n    name=\"Ada\",\n    email=\"ada@example.com\",\n)\n\n# Read one\ncustomer = await Customer.async_queryset.get(id=new_customer.id)\n\n# Read many\ncustomers = await Customer.async_queryset.filter(name__icontains=\"ada\")\n\n# Update rows\nupdated = await Customer.async_queryset.filter(id=customer.id).update(\n    name=\"Ada Lovelace\",\n)\n\n# Delete rows\ndeleted = await Customer.async_queryset.filter(id=customer.id).delete()\n</code></pre>"},{"location":"getting-started/#6-use-instance-helpers","title":"6. Use Instance Helpers","text":"<pre><code>customer = await Customer.async_queryset.get(id=1)\ncustomer.name = \"Updated Name\"\nawait customer.save(columns=[\"name\"])\nawait customer.delete()\n</code></pre>"},{"location":"getting-started/#7-drop-to-raw-sql","title":"7. Drop to Raw SQL","text":"<pre><code>from fastpg import AsyncRawQuery\n\nrecords = await AsyncRawQuery(\n    query=\"SELECT id, email FROM customers WHERE id &gt; :min_id\"\n).fetch(values={\"min_id\": 10})\n</code></pre>"},{"location":"api/database-models/","title":"Database Models API","text":""},{"location":"api/database-models/#databasemodel","title":"<code>DatabaseModel</code>","text":"<p>Base class for all FastPG models.</p>"},{"location":"api/database-models/#class-attributes","title":"Class Attributes","text":"<ul> <li><code>async_queryset</code>   Returns a fresh <code>AsyncQuerySet(model=cls)</code> bound to the current FastPG instance.</li> <li><code>write_connection</code>   Set from <code>get_fastpg().db_conn_manager.db_for_write()</code> when <code>async_queryset</code> is accessed.</li> </ul>"},{"location":"api/database-models/#hooks","title":"Hooks","text":"<ul> <li><code>async pre_save() -&gt; None</code></li> <li><code>async post_save() -&gt; None</code></li> </ul> <p>Both are no-op by default and can be overridden on the model class.</p>"},{"location":"api/database-models/#methods","title":"Methods","text":""},{"location":"api/database-models/#async-savecolumns-liststr-none-none-bool","title":"<code>async save(columns: list[str] | None = None) -&gt; bool</code>","text":"<p>Updates the current row by primary key.</p> <ul> <li>Calls <code>pre_save()</code> before the update query.</li> <li>Applies <code>auto_now_fields</code> when the current field value is <code>None</code>.</li> <li>Calls <code>post_save()</code> only when at least one row is updated.</li> </ul>"},{"location":"api/database-models/#async-delete-bool","title":"<code>async delete() -&gt; bool</code>","text":"<p>Deletes the current row by primary key and returns whether the delete affected a row.</p>"},{"location":"api/database-models/#required-meta-members","title":"Required <code>Meta</code> Members","text":"<ul> <li><code>db_table</code></li> <li><code>primary_key</code></li> </ul>"},{"location":"api/database-models/#optional-meta-members","title":"Optional <code>Meta</code> Members","text":"<ul> <li><code>auto_generated_fields</code></li> <li><code>auto_now_add_fields</code></li> <li><code>auto_now_fields</code></li> <li><code>relations</code></li> </ul>"},{"location":"api/database-models/#queryset_property","title":"<code>queryset_property</code>","text":"<p>Descriptor used internally to expose <code>DatabaseModel.async_queryset</code> as a class-level accessor.</p>"},{"location":"api/database-models/#transaction","title":"<code>Transaction</code>","text":"<p>Utility wrapper around the current FastPG write connection transaction.</p>"},{"location":"api/database-models/#methods_1","title":"Methods","text":"<ul> <li><code>Transaction.atomic()</code></li> <li><code>await Transaction.start()</code></li> <li><code>Transaction.decorator()</code></li> </ul>"},{"location":"api/queries/","title":"Query API","text":""},{"location":"api/queries/#asyncquerysetmodel","title":"<code>AsyncQuerySet(model)</code>","text":"<p>Primary query builder for model reads and writes.</p>"},{"location":"api/queries/#query-construction","title":"Query Construction","text":"Method Purpose <code>using(conn_name)</code> Override the read connection for this queryset. <code>columns(*columns)</code> Limit selected columns. <code>get(*args, **kwargs)</code> Fetch one matching row. <code>filter(*args, **kwargs)</code> Fetch many matching rows. <code>all()</code> Fetch all rows. <code>count()</code> Count rows, optionally after existing filters were added. <code>order_by(**order_by)</code> Add <code>ORDER BY</code>. <code>limit(fetch_limit)</code> Add <code>LIMIT</code>. <code>offset(fetch_offset)</code> Add <code>OFFSET</code>. <code>return_as(return_type)</code> Return model instances or dicts for read queries."},{"location":"api/queries/#relationship-helpers","title":"Relationship Helpers","text":"Method Purpose <code>select_related(*relation_names)</code> Join one configured relation and hydrate it. <code>filter_related(*args, **kwargs)</code> Add <code>WHERE</code> clauses for the joined related table. <code>prefetch_related(*prefetches)</code> Fetch child collections in follow-up queries."},{"location":"api/queries/#write-helpers","title":"Write Helpers","text":"Method Purpose <code>create(**kwargs)</code> Insert one row and return the model instance. <code>bulk_create(values, on_conflict, conflict_target=None, update_fields=None, skip_validations=False)</code> Batch insert or upsert. <code>get_or_create(defaults, **kwargs)</code> Fetch one row or create it. <code>update_or_create(defaults, **kwargs)</code> Update an existing row or create it. <code>update(**kwargs)</code> Build an update query for the current filtered queryset. <code>delete()</code> Build a delete query for the current filtered queryset."},{"location":"api/queries/#raw-execution-helper","title":"Raw Execution Helper","text":"<ul> <li><code>execute_raw_query(query, values)</code>   Runs a raw read query through the queryset's current read connection and returns serialized results.</li> </ul>"},{"location":"api/queries/#await-behavior","title":"Await Behavior","text":"<p>Awaiting the queryset executes SQL and returns:</p> <ul> <li><code>get()</code> -&gt; one model instance or dict</li> <li><code>filter()</code> / <code>all()</code> -&gt; list</li> <li><code>count()</code> -&gt; integer</li> <li><code>update()</code> / <code>delete()</code> -&gt; write-query result, typically an affected-row count</li> </ul>"},{"location":"api/queries/#asyncrawqueryquery-usingnone","title":"<code>AsyncRawQuery(query, using=None)</code>","text":"<p>Wrapper for hand-written SQL with FastPG error handling.</p>"},{"location":"api/queries/#methods","title":"Methods","text":"<ul> <li><code>fetch(values)</code> -&gt; list of dict records using a read connection</li> <li><code>execute(values)</code> -&gt; execute one write query using the write connection</li> <li><code>execute_many(list_of_values)</code> -&gt; execute the same write query for many parameter sets</li> </ul>"},{"location":"api/queries/#inclauseparam","title":"<code>InClauseParam</code>","text":"<p>Use <code>InClauseParam([...])</code> inside raw-query values when a named parameter should expand into an <code>IN (...)</code> clause:</p> <pre><code>from fastpg import AsyncRawQuery, InClauseParam\n\nrows = await AsyncRawQuery(\n    query=\"\"\"\n        SELECT * FROM orders\n        WHERE id IN (:order_ids)\n          AND customer_id IN (:customer_ids)\n    \"\"\"\n).fetch(\n    values={\n        \"order_ids\": InClauseParam([1, 2, 3]),\n        \"customer_ids\": InClauseParam([10, 11]),\n    }\n)\n</code></pre>"},{"location":"api/queries/#asyncpaginatorpage_size-queryset-usingnone","title":"<code>AsyncPaginator(page_size, queryset, using=None)</code>","text":"<p>Paginator for <code>AsyncQuerySet</code>.</p> <ul> <li><code>get_page(page=1, context=None)</code></li> <li><code>get_next_page()</code></li> <li><code>get_previous_page()</code></li> </ul>"},{"location":"api/queries/#rawqueryasyncpaginator","title":"<code>RawQueryAsyncPaginator(...)</code>","text":"<p>Paginator for raw SQL with optional serializer and optional automatic <code>LIMIT</code> / <code>OFFSET</code> handling.</p>"},{"location":"examples/advanced-usage/","title":"Advanced Usage","text":"<p>These patterns come directly from <code>test_project/app/api/endpoints/shop_api.py</code>.</p>"},{"location":"examples/advanced-usage/#select-related-with-related-filtering","title":"Select Related With Related Filtering","text":"<pre><code>from fastpg import OrderBy\n\nemployees = Employee.async_queryset.select_related(\"department\")\nemployees = employees.filter(salary__gte=50000)\nemployees = employees.filter_related(department__name=\"Engineering\")\nrows = await employees.order_by(salary=OrderBy.DESCENDING)\n</code></pre>"},{"location":"examples/advanced-usage/#prefetch-child-collections","title":"Prefetch Child Collections","text":"<pre><code>from fastpg import Prefetch, ReturnType\n\nrows = await (\n    Department.async_queryset\n    .prefetch_related(Prefetch(\"employees\", Employee.async_queryset.all()))\n    .all()\n    .return_as(ReturnType.DICT)\n)\n</code></pre>"},{"location":"examples/advanced-usage/#raw-sql-with-in-parameters","title":"Raw SQL With <code>IN</code> Parameters","text":"<pre><code>from fastpg import AsyncRawQuery, InClauseParam\n\nrows = await AsyncRawQuery(\n    query=\"\"\"\n        SELECT * FROM orders\n        WHERE id IN (:order_ids)\n          AND customer_id IN (:customer_ids)\n          AND total_amount &gt;= :total_amount\n    \"\"\"\n).fetch(\n    values={\n        \"order_ids\": InClauseParam([1, 2, 3]),\n        \"customer_ids\": InClauseParam([10, 11]),\n        \"total_amount\": 100,\n    }\n)\n</code></pre>"},{"location":"examples/advanced-usage/#transaction-pattern","title":"Transaction Pattern","text":"<pre><code>from fastpg import Transaction\n\n\nasync def create_department_with_employees(dept, employees):\n    async with Transaction.atomic():\n        department = await Department.async_queryset.create(**dept)\n        for emp in employees:\n            emp[\"department_id\"] = department.id\n            await Employee.async_queryset.create(**emp)\n    return department\n</code></pre>"},{"location":"examples/advanced-usage/#update_or_create","title":"<code>update_or_create</code>","text":"<pre><code>product, created = await Product.async_queryset.update_or_create(\n    id=1,\n    sku=\"SKU-1\",\n    defaults={\"name\": \"Renamed\", \"price\": 12.0, \"stock_quantity\": 100},\n)\n</code></pre>"},{"location":"examples/advanced-usage/#queryset-pagination","title":"Queryset Pagination","text":"<pre><code>from fastpg import AsyncPaginator\n\npaginator = AsyncPaginator(\n    page_size=20,\n    queryset=Product.async_queryset.all(),\n)\n\npage = await paginator.get_page(1)\n</code></pre>"},{"location":"examples/basic-usage/","title":"Basic Usage","text":"<p>This example follows the same patterns used in <code>test_project</code>.</p> <pre><code>from fastpg import DatabaseModel, JsonData\n\n\nclass Category(DatabaseModel):\n    id: int | None = None\n    name: str\n    description: str\n\n    class Meta:\n        db_table = \"categories\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n\n\nclass Product(DatabaseModel):\n    id: int | None = None\n    sku: str\n    name: str\n    category_id: int | None = None\n    price: float\n    stock_quantity: int\n    properties: JsonData = {}\n\n    class Meta:\n        db_table = \"products\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n</code></pre> <p>Create and fetch:</p> <pre><code>product = await Product.async_queryset.create(\n    sku=\"SKU-1\",\n    name=\"Cap\",\n    category_id=1,\n    price=15.0,\n    stock_quantity=10,\n    properties={\"color\": \"black\"},\n)\n\nsame_product = await Product.async_queryset.get(id=product.id)\nproducts = await Product.async_queryset.all()\n</code></pre> <p>Bulk upsert:</p> <pre><code>from fastpg import OnConflict\n\npayload = [\n    {\n        \"sku\": \"SKU-1\",\n        \"name\": \"Cap\",\n        \"category_id\": 1,\n        \"price\": 15.0,\n        \"stock_quantity\": 10,\n        \"properties\": {\"color\": \"black\"},\n    },\n    {\n        \"sku\": \"SKU-2\",\n        \"name\": \"Tee\",\n        \"category_id\": 1,\n        \"price\": 20.0,\n        \"stock_quantity\": 8,\n        \"properties\": {\"size\": \"L\"},\n    },\n]\n\nawait Product.async_queryset.bulk_create(\n    values=payload,\n    on_conflict=OnConflict.UPDATE,\n    conflict_target=[\"sku\"],\n    update_fields=[\"name\", \"category_id\", \"price\", \"stock_quantity\", \"properties\"],\n)\n</code></pre> <p>Update fields in place:</p> <pre><code>await Product.async_queryset.filter(id=1).update(stock_quantity__add=5)\nawait Product.async_queryset.filter(id=1).update(properties__jsonb_set__color=\"blue\")\n</code></pre>"},{"location":"guides/models/","title":"Models","text":"<p>All FastPG models inherit from <code>DatabaseModel</code>, which extends <code>pydantic.BaseModel</code>.</p>"},{"location":"guides/models/#model-definition","title":"Model Definition","text":"<pre><code>from datetime import datetime\nfrom fastpg import DatabaseModel\n\n\nclass Department(DatabaseModel):\n    id: int | None = None\n    name: str\n    location: str\n    created_at: datetime | None = None\n\n    class Meta:\n        db_table = \"departments\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n        auto_now_add_fields = [\"created_at\"]\n</code></pre>"},{"location":"guides/models/#meta-options","title":"<code>Meta</code> Options","text":"Option Required Meaning <code>db_table</code> Yes PostgreSQL table name. <code>primary_key</code> Yes Primary key column used by <code>save()</code>, <code>delete()</code>, <code>count()</code>, and insert return handling. <code>auto_generated_fields</code> No Fields removed from insert payloads before <code>create()</code> and <code>bulk_create()</code>. <code>auto_now_add_fields</code> No Fields filled with <code>datetime.now(fastpg.TZ)</code> during create flows when the current value is <code>None</code>. <code>auto_now_fields</code> No Fields filled with <code>datetime.now(fastpg.TZ)</code> before <code>save()</code> when the current value is <code>None</code>. <code>relations</code> No Mapping of relation name to <code>Relation(...)</code>."},{"location":"guides/models/#class-level-accessor","title":"Class-Level Accessor","text":"<p><code>async_queryset</code> is a descriptor on the model class:</p> <pre><code>rows = await Department.async_queryset.all()\n</code></pre> <p>Each access returns a fresh <code>AsyncQuerySet</code> bound to the current FastPG instance.</p>"},{"location":"guides/models/#instance-hooks","title":"Instance Hooks","text":"<p>Override these when you need model-specific save hooks:</p> <pre><code>class Department(DatabaseModel):\n    ...\n\n    async def pre_save(self) -&gt; None:\n        ...\n\n    async def post_save(self) -&gt; None:\n        ...\n</code></pre> <p><code>save()</code> calls <code>pre_save()</code> before the update query and <code>post_save()</code> only if the update succeeds.</p>"},{"location":"guides/models/#instance-methods","title":"Instance Methods","text":""},{"location":"guides/models/#savecolumns-liststr-none-none-bool","title":"<code>save(columns: list[str] | None = None) -&gt; bool</code>","text":"<p>Updates the row identified by <code>Meta.primary_key</code>.</p> <ul> <li>Returns <code>True</code> when at least one row is updated.</li> <li>If <code>columns</code> is omitted, FastPG includes every field from <code>model_dump(...)</code> in the <code>SET</code> clause.</li> <li><code>auto_now_fields</code> are only filled when the current field value is <code>None</code>.</li> </ul>"},{"location":"guides/models/#delete-bool","title":"<code>delete() -&gt; bool</code>","text":"<p>Deletes the row identified by <code>Meta.primary_key</code> and returns whether a row was deleted.</p>"},{"location":"guides/models/#json-columns","title":"JSON Columns","text":"<p>Use <code>JsonData</code> for JSON or JSONB-backed fields:</p> <pre><code>from fastpg import DatabaseModel, JsonData\n\n\nclass Product(DatabaseModel):\n    id: int | None = None\n    name: str\n    properties: JsonData = {}\n\n    class Meta:\n        db_table = \"products\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n</code></pre> <p><code>JsonData</code> keeps Python dict/list values in normal model use and serializes them when FastPG prepares DB writes.</p>"},{"location":"guides/pagination/","title":"Pagination","text":"<p>FastPG provides:</p> <ul> <li><code>AsyncPaginator</code> for <code>AsyncQuerySet</code></li> <li><code>RawQueryAsyncPaginator</code> for raw SQL</li> </ul> <p>Both return this shape:</p> <pre><code>{\n  \"results\": [],\n  \"results_paginator\": {\n    \"number\": 1,\n    \"page_size\": 20,\n    \"has_next\": false,\n    \"has_previous\": false,\n    \"start_index\": 0,\n    \"end_index\": 0\n  }\n}\n</code></pre>"},{"location":"guides/pagination/#asyncpaginator","title":"<code>AsyncPaginator</code>","text":"<pre><code>from fastpg import AsyncPaginator, OrderBy\n\npaginator = AsyncPaginator(\n    page_size=25,\n    queryset=Product.async_queryset.all().order_by(id=OrderBy.ASCENDING),\n)\n\npage1 = await paginator.get_page(page=1)\npage2 = await paginator.get_next_page()\n</code></pre> <p>Behavior:</p> <ul> <li><code>page</code> must be <code>&gt;= 1</code>, otherwise <code>InvalidPageError</code>.</li> <li><code>get_page()</code> applies <code>limit(page_size)</code> and <code>offset((page - 1) * page_size)</code>.</li> <li>Pass <code>using=\"replica_1\"</code> to run on a specific read connection.</li> </ul> <p>Metadata notes:</p> <ul> <li><code>has_next</code> is inferred from whether the current page returned exactly <code>page_size</code> rows.</li> <li><code>start_index</code> is zero-based.</li> <li><code>end_index</code> is <code>start_index + object_count</code>.</li> </ul>"},{"location":"guides/pagination/#rawqueryasyncpaginator","title":"<code>RawQueryAsyncPaginator</code>","text":"<pre><code>from fastpg import RawQueryAsyncPaginator\n\npaginator = RawQueryAsyncPaginator(\n    page_size=50,\n    query=\"SELECT id, sku FROM products ORDER BY id\",\n    values={},\n    serializer=lambda rows: [{\"id\": r[\"id\"], \"sku\": r[\"sku\"]} for r in rows],\n)\n\npage = await paginator.get_page(page=1)\n</code></pre> <p>Behavior:</p> <ul> <li><code>auto_offset_and_limit=True</code> appends <code>LIMIT ... OFFSET ...</code>.</li> <li><code>auto_offset_and_limit=False</code> expects <code>{page_size}</code> and <code>{offset}</code> placeholders inside your query string.</li> <li><code>serializer</code> runs after the raw fetch and before the paginator response is built.</li> </ul>"},{"location":"guides/querysets/","title":"Querysets","text":"<p><code>AsyncQuerySet</code> builds SQL lazily and executes only when awaited.</p>"},{"location":"guides/querysets/#execution-model","title":"Execution Model","text":"<p>A queryset becomes awaitable after one of these terminal actions sets the operation:</p> <ul> <li><code>get(...)</code></li> <li><code>filter(...)</code></li> <li><code>all()</code></li> <li><code>count()</code></li> <li><code>update(...)</code></li> <li><code>delete()</code></li> </ul> <p>Awaiting a queryset with no action raises <code>MalformedQuerysetError</code>.</p>"},{"location":"guides/querysets/#basic-reads","title":"Basic Reads","text":"<pre><code>user = await User.async_queryset.get(id=1)\nusers = await User.async_queryset.filter(name__icontains=\"ada\")\nall_users = await User.async_queryset.all()\ntotal = await User.async_queryset.filter(is_active=True).count()\n</code></pre> <p><code>get(...)</code> returns one record or raises <code>DoesNotExist</code> / <code>MultipleRecordsFound</code>.</p>"},{"location":"guides/querysets/#lookup-operators","title":"Lookup Operators","text":"<p>Supported filter suffixes:</p> <ul> <li><code>__gt</code>, <code>__lt</code>, <code>__gte</code>, <code>__lte</code>, <code>__ne</code></li> <li><code>__in</code></li> <li><code>__isnull</code></li> <li><code>__contains</code>, <code>__icontains</code></li> <li><code>__startswith</code>, <code>__istartswith</code></li> <li><code>__endswith</code>, <code>__iendswith</code></li> </ul> <p>Notes:</p> <ul> <li><code>__in</code> requires a non-empty Python <code>list</code>.</li> <li><code>__isnull=True</code> renders <code>IS NULL</code>; <code>False</code> renders <code>IS NOT NULL</code>.</li> </ul>"},{"location":"guides/querysets/#q-objects","title":"<code>Q</code> Objects","text":"<pre><code>from fastpg import Q\n\nq = Q(city=\"Boston\") | Q(city=\"New York\")\nrows = await Customer.async_queryset.filter(q, is_active=True)\n</code></pre> <p>Positional <code>Q(...)</code> objects and keyword filters are combined with <code>AND</code>.</p>"},{"location":"guides/querysets/#selecting-columns-and-return-type","title":"Selecting Columns and Return Type","text":"<pre><code>from fastpg import OrderBy, ReturnType\n\nrows = await (\n    Product.async_queryset\n    .columns(\"id\", \"name\", \"price\")\n    .filter(price__gte=100)\n    .order_by(price=OrderBy.DESCENDING)\n    .return_as(ReturnType.DICT)\n)\n</code></pre> <p>Use <code>return_as(ReturnType.DICT)</code> when fetching partial columns unless the omitted model fields all have defaults. Model-instance hydration still goes through the model constructor.</p>"},{"location":"guides/querysets/#read-connection-selection","title":"Read Connection Selection","text":"<pre><code>rows = await Product.async_queryset.using(\"replica_1\").all()\n</code></pre> <p><code>using(conn_name)</code> only changes the read connection used by that queryset.</p>"},{"location":"guides/querysets/#write-helpers","title":"Write Helpers","text":""},{"location":"guides/querysets/#createkwargs","title":"<code>create(**kwargs)</code>","text":"<p>Creates one row and returns a model instance.</p> <ul> <li><code>auto_now_add_fields</code> are populated before insert when the field is <code>None</code>.</li> <li><code>auto_generated_fields</code> are removed from the insert payload.</li> <li>Conflict handling is not part of <code>create()</code>.</li> </ul>"},{"location":"guides/querysets/#bulk_createvalues-on_conflict-conflict_targetnone-update_fieldsnone-skip_validationsfalse","title":"<code>bulk_create(values, on_conflict, conflict_target=None, update_fields=None, skip_validations=False)</code>","text":"<p>Creates multiple rows with one statement template executed through <code>execute_many()</code>.</p> <pre><code>from fastpg import OnConflict\n\nawait Product.async_queryset.bulk_create(\n    values=products_batch,\n    on_conflict=OnConflict.UPDATE,\n    conflict_target=[\"sku\"],\n    update_fields=[\"name\", \"category_id\", \"price\", \"stock_quantity\"],\n)\n</code></pre> <p>Behavior:</p> <ul> <li><code>values=[]</code> raises <code>NothingToCreateError</code>.</li> <li><code>OnConflict.DO_NOTHING</code> adds <code>ON CONFLICT DO NOTHING</code>.</li> <li><code>OnConflict.UPDATE</code> requires both <code>conflict_target</code> and <code>update_fields</code>.</li> <li>Passing <code>None</code> for <code>on_conflict</code> produces a plain batch insert with no conflict clause.</li> <li><code>skip_validations=True</code> uses <code>model_construct(...)</code> instead of normal Pydantic validation.</li> </ul>"},{"location":"guides/querysets/#get_or_createdefaults-lookup","title":"<code>get_or_create(defaults, **lookup)</code>","text":"<p>Fetches a row by <code>lookup</code> or creates one from <code>{**lookup, **defaults}</code>.</p> <p>Returns <code>(obj, created)</code>.</p>"},{"location":"guides/querysets/#update_or_createdefaults-lookup","title":"<code>update_or_create(defaults, **lookup)</code>","text":"<p>Fetches a row by <code>lookup</code>, updates it with <code>defaults</code> via model <code>save()</code>, or creates a new row.</p> <p>Returns <code>(obj, created)</code>.</p>"},{"location":"guides/querysets/#update","title":"<code>update(...)</code>","text":"<p>Always start with a filtered queryset:</p> <pre><code>updated = await Product.async_queryset.filter(id=1).update(name=\"New Name\")\n</code></pre> <p>Supported update suffixes:</p> <ul> <li>Arithmetic: <code>__add</code>, <code>__sub</code>, <code>__mul</code>, <code>__div</code></li> <li>Time intervals: <code>__add_time</code>, <code>__sub_time</code></li> <li>JSONB replace: <code>__jsonb</code></li> <li>JSONB set key: <code>__jsonb_set__key_name</code></li> <li>JSONB remove key: <code>__jsonb_remove</code></li> </ul> <p>Example:</p> <pre><code>await Product.async_queryset.filter(id=1).update(\n    stock_quantity__add=5,\n    properties__jsonb_set__color=\"blue\",\n)\n</code></pre>"},{"location":"guides/querysets/#delete","title":"<code>delete()</code>","text":"<p>Always start with a filtered queryset:</p> <pre><code>deleted = await Product.async_queryset.filter(id=1).delete()\n</code></pre>"},{"location":"guides/querysets/#safety-note","title":"Safety Note","text":"<p><code>UnrestrictedUpdateError</code> and <code>UnrestrictedDeleteError</code> exist for unsafe mutations, but the current queryset flow should still be treated as \"filter first, then mutate\" to guarantee valid SQL generation.</p>"},{"location":"guides/relationships/","title":"Relationships","text":"<p>FastPG relationships are explicit. You describe them in <code>Meta.relations</code> and opt into loading strategies per query.</p>"},{"location":"guides/relationships/#define-a-relation","title":"Define a Relation","text":"<pre><code>from fastpg import DatabaseModel, Relation\n\n\nclass Department(DatabaseModel):\n    id: int\n    name: str\n\n    class Meta:\n        db_table = \"departments\"\n        primary_key = \"id\"\n\n\nclass Employee(DatabaseModel):\n    id: int\n    department_id: int | None = None\n    name: str\n\n    class Meta:\n        db_table = \"employees\"\n        primary_key = \"id\"\n        relations = {\n            \"department\": Relation(Department, foreign_field=\"department_id\"),\n        }\n</code></pre> <p><code>foreign_field</code> is the column on the base model. FastPG renders the join as:</p> <pre><code>t.department_id = r.id\n</code></pre> <p>If you omit <code>related_name</code>, FastPG derives one from the related model class name in snake case.</p>"},{"location":"guides/relationships/#select_related","title":"<code>select_related(...)</code>","text":"<p><code>select_related()</code> performs a <code>LEFT JOIN</code> and hydrates one related object per base row.</p> <pre><code>employee = await Employee.async_queryset.select_related(\"department\").get(id=1)\n</code></pre> <p>Current behavior:</p> <ul> <li>Only one relation is used.</li> <li>If multiple names are passed, FastPG uses the first one.</li> <li>Missing relation names raise <code>InvalidRelatedFieldError</code>.</li> </ul>"},{"location":"guides/relationships/#filter_related","title":"<code>filter_related(...)</code>","text":"<p>Use <code>filter_related()</code> after <code>select_related()</code> to add <code>WHERE</code> conditions against the joined table:</p> <pre><code>from fastpg import OrderBy\n\nrows = await (\n    Employee.async_queryset\n    .select_related(\"department\")\n    .filter(salary__gte=50000)\n    .filter_related(department__name=\"Engineering\")\n    .order_by(salary=OrderBy.DESCENDING)\n)\n</code></pre> <p>Related-field filters use the relation name as the prefix, for example <code>department__name=...</code>.</p>"},{"location":"guides/relationships/#prefetch_related","title":"<code>prefetch_related(...)</code>","text":"<p><code>prefetch_related()</code> runs the base query first, then executes additional filtered queries and attaches child collections to each base object.</p> <pre><code>from fastpg import Prefetch, ReturnType\n\nrows = await (\n    Department.async_queryset\n    .prefetch_related(Prefetch(\"employees\", Employee.async_queryset.all()))\n    .all()\n    .return_as(ReturnType.DICT)\n)\n</code></pre> <p>Requirements:</p> <ul> <li>The prefetched queryset model must define a <code>Relation(...)</code> back to the base model.</li> <li>Otherwise FastPG raises <code>InvalidPrefetchError</code>.</li> </ul> <p>Behavior:</p> <ul> <li>For model-instance results, FastPG sets an attribute named by <code>dataset_name</code>.</li> <li>For <code>ReturnType.DICT</code>, FastPG inserts a new key with that dataset name.</li> </ul>"},{"location":"guides/relationships/#choosing-a-strategy","title":"Choosing a Strategy","text":"<ul> <li>Use <code>select_related</code> for many-to-one or one-to-one style joins.</li> <li>Use <code>prefetch_related</code> for one-to-many child collections.</li> </ul>"},{"location":"guides/transactions/","title":"Transactions","text":"<p>FastPG exposes write-side transaction helpers through <code>Transaction</code>.</p>"},{"location":"guides/transactions/#context-manager","title":"Context Manager","text":"<pre><code>from fastpg import Transaction\n\n\nasync def create_department_and_employees(dept_data, employees_data):\n    async with Transaction.atomic():\n        department = await Department.async_queryset.create(**dept_data)\n        for emp in employees_data:\n            emp[\"department_id\"] = department.id\n            await Employee.async_queryset.create(**emp)\n    return department\n</code></pre> <p>This is the safest pattern and matches the sample FastAPI project.</p>"},{"location":"guides/transactions/#manual-flow","title":"Manual Flow","text":"<pre><code>transaction = await Transaction.start()\ntry:\n    await Order.async_queryset.create(\n        customer_id=1,\n        order_date=\"2025-01-01\",\n        total_amount=99.5,\n        status=\"PENDING\",\n    )\nexcept Exception:\n    await transaction.rollback()\n    raise\nelse:\n    await transaction.commit()\n</code></pre>"},{"location":"guides/transactions/#decorator-style","title":"Decorator Style","text":"<pre><code>from fastpg import Transaction\n\n\n@Transaction.decorator()\nasync def create_coupon(payload):\n    return await Coupon.async_queryset.create(**payload)\n</code></pre>"},{"location":"guides/transactions/#notes","title":"Notes","text":"<ul> <li>Transactions are taken from the configured write connection.</li> <li>Call <code>connect_all()</code> before using transaction helpers.</li> <li><code>Transaction.atomic()</code> returns the underlying transaction context manager.</li> </ul>"},{"location":"reference/constants/","title":"Constants And Helpers","text":""},{"location":"reference/constants/#constants","title":"Constants","text":""},{"location":"reference/constants/#connectiontype","title":"<code>ConnectionType</code>","text":"<ul> <li><code>ConnectionType.READ</code></li> <li><code>ConnectionType.WRITE</code></li> </ul>"},{"location":"reference/constants/#orderby","title":"<code>OrderBy</code>","text":"<ul> <li><code>OrderBy.ASCENDING</code></li> <li><code>OrderBy.DESCENDING</code></li> </ul> <p>These render to <code>ASC</code> and <code>DESC</code>.</p>"},{"location":"reference/constants/#onconflict","title":"<code>OnConflict</code>","text":"<ul> <li><code>OnConflict.DO_NOTHING</code></li> <li><code>OnConflict.UPDATE</code></li> </ul> <p>Used by <code>bulk_create(...)</code>.</p>"},{"location":"reference/constants/#returntype","title":"<code>ReturnType</code>","text":"<ul> <li><code>ReturnType.MODEL_INSTANCE</code></li> <li><code>ReturnType.DICT</code></li> </ul> <p>Used by queryset <code>return_as(...)</code>.</p>"},{"location":"reference/constants/#relationship-helpers","title":"Relationship Helpers","text":""},{"location":"reference/constants/#relationrelated_model-foreign_field-related_namenone","title":"<code>Relation(related_model, foreign_field, related_name=None)</code>","text":"<p>Stores join metadata for <code>select_related(...)</code>.</p> <ul> <li><code>related_model</code> is the joined model class.</li> <li><code>foreign_field</code> is the foreign-key column on the base model.</li> <li><code>related_name</code> overrides the attribute/key name attached to the result.</li> </ul>"},{"location":"reference/constants/#prefetchdataset_name-queryset","title":"<code>Prefetch(dataset_name, queryset)</code>","text":"<p>Stores child-collection metadata for <code>prefetch_related(...)</code>.</p> <ul> <li><code>dataset_name</code> becomes the attached attribute or dict key.</li> <li><code>queryset</code> is the child model queryset to execute for the prefetch.</li> </ul>"},{"location":"reference/constants/#query-helpers","title":"Query Helpers","text":""},{"location":"reference/constants/#q","title":"<code>Q(...)</code>","text":"<p>Builds reusable SQL <code>WHERE</code> fragments and supports <code>&amp;</code> / <code>|</code> composition.</p>"},{"location":"reference/constants/#inclauseparamvalues","title":"<code>InClauseParam(values)</code>","text":"<p>Expands a single named raw-query parameter into multiple bound values for <code>IN (...)</code> clauses.</p>"},{"location":"reference/errors/","title":"Errors","text":"<p>FastPG defines explicit exception classes in <code>fastpg.errors</code>.</p>"},{"location":"reference/errors/#configuration-and-connection","title":"Configuration And Connection","text":"<ul> <li><code>ReadConnectionNotAvailableError</code></li> <li><code>MultipleWriteConnectionsError</code></li> <li><code>InvalidConnectionNameError</code></li> <li><code>FastPGInstanceNotConfiguredError</code></li> <li><code>MalformedMetaError</code></li> <li><code>MalformedQuerysetError</code></li> </ul>"},{"location":"reference/errors/#query-construction","title":"Query Construction","text":"<ul> <li><code>InvalidINClauseValueError</code></li> <li><code>UnsupportedOperatorError</code></li> <li><code>InvalidRelatedFieldError</code></li> <li><code>InvalidPrefetchError</code></li> </ul>"},{"location":"reference/errors/#result-cardinality","title":"Result Cardinality","text":"<ul> <li><code>DoesNotExist</code></li> <li><code>MultipleRecordsFound</code></li> </ul>"},{"location":"reference/errors/#write-and-pagination","title":"Write And Pagination","text":"<ul> <li><code>NothingToCreateError</code></li> <li><code>UnrestrictedUpdateError</code></li> <li><code>UnrestrictedDeleteError</code></li> <li><code>InvalidPageError</code></li> </ul>"},{"location":"reference/errors/#database-execution","title":"Database Execution","text":"<ul> <li><code>DatabaseError</code></li> <li><code>DuplicateKeyDatabaseError</code></li> </ul> <p><code>DatabaseError</code> wraps the driver error details and includes the SQLSTATE when one is available.</p>"},{"location":"reference/json-fields/","title":"JSON Fields","text":"<p>FastPG exposes JSON helpers in <code>fastpg.fields</code>.</p>"},{"location":"reference/json-fields/#jsondata","title":"<code>JsonData</code>","text":"<p><code>JsonData</code> is an annotated Pydantic type for JSON-compatible values.</p> <pre><code>from fastpg import DatabaseModel, JsonData\n\n\nclass Product(DatabaseModel):\n    id: int | None = None\n    properties: JsonData = {}\n\n    class Meta:\n        db_table = \"products\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n</code></pre> <p>Behavior:</p> <ul> <li>Dicts and lists are serialized to JSON strings during DB writes.</li> <li>Normal model usage keeps Python values as dict/list objects.</li> <li><code>CustomJsonEncoder</code> serializes <code>datetime</code> values with <code>isoformat()</code>.</li> </ul>"},{"location":"reference/json-fields/#helper-functions","title":"Helper Functions","text":"<ul> <li><code>json_str_to_dict(value)</code>   Parses JSON strings returned from the database and leaves non-string values unchanged.</li> <li><code>validate_json_data(data)</code>   Converts dict/list values to JSON during validation.</li> <li><code>serialize_json_data(data, info)</code>   Returns JSON strings only when <code>info.context[\"db_write\"]</code> is truthy.</li> </ul>"},{"location":"reference/json-fields/#jsonb-update-operators","title":"JSONB Update Operators","text":"<p>FastPG supports these queryset update forms:</p> <ul> <li><code>field__jsonb={...}</code></li> <li><code>field__jsonb_set__key=value</code></li> <li><code>field__jsonb_remove=\"key\"</code></li> </ul> <p>Example:</p> <pre><code>await Product.async_queryset.filter(id=1).update(\n    properties__jsonb_set__color=\"blue\",\n)\n</code></pre>"},{"location":"reference/preprocessors/","title":"Preprocessors","text":"<p>FastPG applies small preprocessing helpers around create and save operations.</p>"},{"location":"reference/preprocessors/#precreateprocessors","title":"<code>PreCreateProcessors</code>","text":""},{"location":"reference/preprocessors/#model_obj_populate_auto_now_add_fieldsmodel_obj","title":"<code>model_obj_populate_auto_now_add_fields(model_obj)</code>","text":"<ul> <li>Reads <code>model_obj.Meta.auto_now_add_fields</code>.</li> <li>For each listed field, sets <code>datetime.now(fastpg.TZ)</code> only when the current value is <code>None</code>.</li> </ul>"},{"location":"reference/preprocessors/#model_dict_populate_auto_generated_fieldsmodel_dict-model_cls","title":"<code>model_dict_populate_auto_generated_fields(model_dict, model_cls)</code>","text":"<ul> <li>Reads <code>model_cls.Meta.auto_generated_fields</code>.</li> <li>Deletes those keys from the insert payload before SQL execution.</li> </ul>"},{"location":"reference/preprocessors/#presaveprocessors","title":"<code>PreSaveProcessors</code>","text":""},{"location":"reference/preprocessors/#model_obj_populate_auto_now_fieldsmodel_obj","title":"<code>model_obj_populate_auto_now_fields(model_obj)</code>","text":"<ul> <li>Reads <code>model_obj.Meta.auto_now_fields</code>.</li> <li>For each listed field, sets <code>datetime.now(fastpg.TZ)</code> only when the current value is <code>None</code>.</li> </ul>"},{"location":"reference/preprocessors/#where-they-are-used","title":"Where They Are Used","text":"<ul> <li><code>AsyncQuerySet.create(...)</code></li> <li><code>AsyncQuerySet.bulk_create(...)</code></li> <li><code>DatabaseModel.save(...)</code></li> </ul> <p>If the optional <code>Meta</code> lists are missing, these helpers simply do nothing.</p>"},{"location":"reference/settings/","title":"Settings And Configuration","text":""},{"location":"reference/settings/#create_fastpg","title":"<code>create_fastpg(...)</code>","text":"<p>Primary entry point:</p> <pre><code>from fastpg import create_fastpg\n\ncreate_fastpg(\n    name=\"default\",\n    databases={...},\n    tz_name=\"UTC\",\n    query_logger={\"LOG_QUERIES\": True, \"TITLE\": \"MY_APP\"},\n    db_conn_manager_class=None,\n)\n</code></pre> <p>FastPG does not load environment variables by itself. Your application is responsible for building the <code>databases</code> config dict.</p>"},{"location":"reference/settings/#database-config-format","title":"Database Config Format","text":"<p>Each configured connection uses:</p> <ul> <li><code>TYPE</code>: <code>ConnectionType.READ</code> or <code>ConnectionType.WRITE</code></li> <li><code>USER</code></li> <li><code>PASSWORD</code></li> <li><code>DB</code></li> <li><code>HOST</code></li> <li><code>PORT</code></li> </ul> <p>Example:</p> <pre><code>from fastpg import ConnectionType\n\ndatabases = {\n    \"default\": {\n        \"TYPE\": ConnectionType.WRITE,\n        \"USER\": \"postgres\",\n        \"PASSWORD\": \"postgres\",\n        \"DB\": \"app_db\",\n        \"HOST\": \"127.0.0.1\",\n        \"PORT\": 5432,\n    },\n    \"replica_1\": {\n        \"TYPE\": ConnectionType.READ,\n        \"USER\": \"postgres\",\n        \"PASSWORD\": \"postgres\",\n        \"DB\": \"app_db\",\n        \"HOST\": \"127.0.0.1\",\n        \"PORT\": 5433,\n    },\n}\n</code></pre> <p>Validation behavior:</p> <ul> <li>At least one read connection is required.</li> <li>More than one write connection raises <code>MultipleWriteConnectionsError</code>.</li> </ul>"},{"location":"reference/settings/#timezone","title":"Timezone","text":"<ul> <li><code>tz_name</code> controls the timezone used by auto timestamp preprocessors.</li> <li>Invalid timezone names fall back to <code>UTC</code>.</li> </ul>"},{"location":"reference/settings/#query-logging","title":"Query Logging","text":"<p>Enable DB timing logs with:</p> <pre><code>query_logger = {\n    \"LOG_QUERIES\": True,\n    \"TITLE\": \"SHOP_API\",\n}\n</code></pre> <p>When enabled, FastPG logs through <code>fastpg.utils</code> and prefixes entries with the configured title plus an elapsed-time bucket.</p>"},{"location":"reference/settings/#registry-helpers","title":"Registry Helpers","text":"<p>FastPG supports multiple named instances:</p> <ul> <li><code>register_fastpg(name, instance)</code></li> <li><code>create_fastpg(name=...)</code></li> <li><code>get_fastpg(name=None)</code></li> <li><code>set_current_fastpg(name)</code></li> </ul> <p><code>get_fastpg()</code> without a name uses the current context-bound instance.</p>"},{"location":"reference/settings/#dbconnectionmanager","title":"<code>DBConnectionManager</code>","text":"<p>Methods:</p> <ul> <li><code>connect_all()</code></li> <li><code>close_all()</code></li> <li><code>get_db_conn(conn_name)</code></li> <li><code>db_for_read()</code></li> <li><code>db_for_write()</code></li> </ul> <p>Routing behavior:</p> <ul> <li><code>db_for_read()</code> randomly selects one configured read connection.</li> <li><code>db_for_write()</code> always returns the single configured write connection.</li> </ul>"}]}