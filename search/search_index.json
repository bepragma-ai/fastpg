{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"FastPG","text":"<p>FastPG is a lightweight async ORM for PostgreSQL built on top of <code>pydantic</code> and <code>databases</code>. It keeps SQL explicit, but removes repetitive CRUD and serialization plumbing.</p>"},{"location":"#what-fastpg-gives-you","title":"What FastPG gives you","text":"<ul> <li>Pydantic-based models (<code>DatabaseModel</code>) with async CRUD helpers.</li> <li>Chainable query builder (<code>AsyncQuerySet</code>) with Django-style lookup operators.</li> <li>Lightweight relationship loading (<code>select_related</code>, <code>prefetch_related</code>).</li> <li>Bulk inserts with optional PostgreSQL conflict handling.</li> <li>JSON field helpers and JSONB update operators.</li> <li>Built-in paginators for querysets and raw SQL.</li> <li>Transaction helpers and query timing logs.</li> </ul>"},{"location":"#project-status-notes","title":"Project status notes","text":"<p>This documentation reflects the current source in <code>src/fastpg</code>.</p> <ul> <li><code>create(..., on_conflict=...)</code> currently accepts conflict args but does not apply an <code>ON CONFLICT</code> clause.</li> <li><code>bulk_create(..., on_conflict=...)</code> does support <code>OnConflict.DO_NOTHING</code> and <code>OnConflict.UPDATE</code>.</li> <li><code>update()</code> and <code>delete()</code> should always be chained after <code>filter(...)</code> to ensure a valid <code>WHERE</code> clause.</li> </ul>"},{"location":"#minimal-example","title":"Minimal example","text":"<pre><code>from fastpg import DatabaseModel\n\n\nclass User(DatabaseModel):\n    id: int | None = None\n    email: str\n\n    class Meta:\n        db_table = \"users\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n\n\nasync def list_users():\n    return await User.async_queryset.all()\n</code></pre>"},{"location":"#read-next","title":"Read next","text":"<ul> <li><code>getting-started.md</code></li> <li><code>guides/models.md</code></li> <li><code>guides/querysets.md</code></li> <li><code>guides/relationships.md</code></li> <li><code>guides/pagination.md</code></li> <li><code>guides/transactions.md</code></li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#1-install","title":"1. Install","text":"<p>FastPG is currently installed from GitHub:</p> <pre><code>pip install git+https://github.com/bepragma-ai/fastpg.git\n</code></pre>"},{"location":"getting-started/#2-configure-fastpg","title":"2. Configure FastPG","text":"<p>Create one FastPG instance at app startup.</p> <pre><code>import os\nfrom fastapi import FastAPI\nfrom fastpg import create_fastpg, ConnectionType\n\napp = FastAPI()\n\nFAST_PG = create_fastpg(\n    name=\"default\",\n    databases={\n        \"primary\": {\n            \"TYPE\": ConnectionType.WRITE,\n            \"USER\": os.environ[\"POSTGRES_WRITE_USER\"],\n            \"PASSWORD\": os.environ[\"POSTGRES_WRITE_PASSWORD\"],\n            \"DB\": os.environ[\"POSTGRES_WRITE_DB\"],\n            \"HOST\": os.environ[\"POSTGRES_WRITE_HOST\"],\n            \"PORT\": os.environ[\"POSTGRES_WRITE_PORT\"],\n        },\n        \"replica_1\": {\n            \"TYPE\": ConnectionType.READ,\n            \"USER\": os.environ[\"POSTGRES_READ_USER\"],\n            \"PASSWORD\": os.environ[\"POSTGRES_READ_PASSWORD\"],\n            \"DB\": os.environ[\"POSTGRES_READ_DB\"],\n            \"HOST\": os.environ[\"POSTGRES_READ_HOST\"],\n            \"PORT\": os.environ[\"POSTGRES_READ_PORT\"],\n        },\n    },\n    tz_name=\"UTC\",\n    query_logger={\n        \"LOG_QUERIES\": True,\n        \"TITLE\": \"MY_SERVICE\",\n    },\n)\n</code></pre> <p>Connection rules:</p> <ul> <li>At least one <code>ConnectionType.READ</code> connection is required.</li> <li>Only one <code>ConnectionType.WRITE</code> connection should be configured.</li> <li>Reads route to a random read connection.</li> <li>Writes always route to the write connection.</li> </ul>"},{"location":"getting-started/#3-connect-and-close-pools","title":"3. Connect and close pools","text":"<pre><code>@app.on_event(\"startup\")\nasync def on_startup():\n    await FAST_PG.db_conn_manager.connect_all()\n\n\n@app.on_event(\"shutdown\")\nasync def on_shutdown():\n    await FAST_PG.db_conn_manager.close_all()\n</code></pre>"},{"location":"getting-started/#4-define-your-first-model","title":"4. Define your first model","text":"<pre><code>from datetime import datetime\nfrom fastpg import DatabaseModel\n\n\nclass Customer(DatabaseModel):\n    id: int | None = None\n    name: str\n    email: str\n    created_at: datetime | None = None\n\n    class Meta:\n        db_table = \"customers\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n        auto_now_add_fields = [\"created_at\"]\n</code></pre>"},{"location":"getting-started/#5-run-crud-queries","title":"5. Run CRUD queries","text":"<pre><code># Create\nnew_customer = await Customer.async_queryset.create(name=\"Ada\", email=\"ada@example.com\")\n\n# Read one\ncustomer = await Customer.async_queryset.get(id=new_customer.id)\n\n# Read many\ncustomers = await Customer.async_queryset.filter(name__icontains=\"a\")\n\n# Update rows (always filter first)\nupdated = await Customer.async_queryset.filter(id=customer.id).update(name=\"Ada Lovelace\")\n\n# Delete rows (always filter first)\ndeleted = await Customer.async_queryset.filter(id=customer.id).delete()\n</code></pre>"},{"location":"getting-started/#6-use-model-instance-helpers","title":"6. Use model instance helpers","text":"<pre><code>customer = await Customer.async_queryset.get(id=1)\ncustomer.name = \"Updated Name\"\nawait customer.save(columns=[\"name\"])\nawait customer.delete()\n</code></pre>"},{"location":"getting-started/#7-use-raw-sql-when-needed","title":"7. Use raw SQL when needed","text":"<pre><code>from fastpg import AsyncRawQuery\n\nrecords = await AsyncRawQuery(\n    query=\"SELECT id, email FROM customers WHERE id &gt; :min_id\"\n).fetch(values={\"min_id\": 10})\n</code></pre>"},{"location":"api/database-models/","title":"Database Models API","text":""},{"location":"api/database-models/#databasemodel","title":"<code>DatabaseModel</code>","text":"<p>Base class for all FastPG models.</p>"},{"location":"api/database-models/#class-attributes","title":"Class attributes","text":"<ul> <li><code>async_queryset</code>: descriptor returning a new <code>AsyncQuerySet(model=cls)</code>.</li> <li><code>write_connection</code>: populated from the current FastPG instance.</li> </ul>"},{"location":"api/database-models/#methods","title":"Methods","text":""},{"location":"api/database-models/#async-def-savecolumns-liststr-none-none-bool","title":"<code>async def save(columns: list[str] | None = None) -&gt; bool</code>","text":"<p>Updates current row by primary key and returns whether any row changed.</p>"},{"location":"api/database-models/#async-def-delete-bool","title":"<code>async def delete() -&gt; bool</code>","text":"<p>Deletes current row by primary key and returns whether any row was deleted.</p>"},{"location":"api/database-models/#required-meta-members","title":"Required <code>Meta</code> members","text":"<ul> <li><code>db_table</code></li> <li><code>primary_key</code></li> </ul>"},{"location":"api/database-models/#optional-meta-members","title":"Optional <code>Meta</code> members","text":"<ul> <li><code>auto_generated_fields</code></li> <li><code>auto_now_add_fields</code></li> <li><code>auto_now_fields</code></li> <li><code>relations</code></li> </ul>"},{"location":"api/database-models/#queryset_property","title":"<code>queryset_property</code>","text":"<p>Descriptor used internally for <code>DatabaseModel.async_queryset</code>.</p>"},{"location":"api/database-models/#transaction","title":"<code>Transaction</code>","text":"<p>Utility class exposing write-connection transaction helpers.</p>"},{"location":"api/database-models/#methods_1","title":"Methods","text":"<ul> <li><code>Transaction.atomic()</code></li> <li><code>await Transaction.start()</code></li> <li><code>Transaction.decorator()</code></li> </ul>"},{"location":"api/queries/","title":"Query API","text":""},{"location":"api/queries/#asyncquerysetmodel","title":"<code>AsyncQuerySet(model)</code>","text":"<p>Main query builder for model operations.</p>"},{"location":"api/queries/#core-methods","title":"Core methods","text":"<p>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD - <code>using(conn_name)</code> - <code>columns(*columns)</code> - <code>get(*conditions, **filters)</code> - <code>filter(*conditions, **filters)</code> - <code>all()</code> - <code>count()</code> - <code>order_by(**order_by)</code> - <code>limit(fetch_limit)</code> - <code>offset(fetch_offset)</code> - <code>return_as(return_type)</code></p>"},{"location":"api/queries/#relationship-methods","title":"Relationship methods","text":"<p>======= | Method | Purpose | |--------|---------| | <code>create(**kwargs)</code> | Insert a single row and return the model instance. | | <code>bulk_create(values, on_conflict, conflict_target=None, update_fields=None, skip_validations=False)</code> | Batch insert. Pass <code>OnConflict.DO_NOTHING</code> to ignore duplicates or <code>OnConflict.UPDATE</code> to upsert matching rows. | | <code>get_or_create(defaults=None, **lookup)</code> | Return an existing row or create one from <code>defaults</code>. |</p>"},{"location":"api/queries/#bulk_create","title":"<code>bulk_create</code>","text":"<p><code>bulk_create()</code> accepts a list of dictionaries and writes them with a single statement template executed through <code>execute_many()</code>.</p> <ul> <li><code>values</code> must be a non-empty <code>list[dict]</code>. An empty list raises <code>NothingToCreateError</code>.</li> <li><code>on_conflict</code> controls whether FastPG emits an <code>ON CONFLICT</code> clause. Pass   <code>None</code> explicitly for a plain batch insert, <code>OnConflict.DO_NOTHING</code> to ignore   duplicates, or <code>OnConflict.UPDATE</code> to perform an upsert.</li> <li><code>conflict_target</code> and <code>update_fields</code> are required when   <code>on_conflict=OnConflict.UPDATE</code>.</li> <li><code>skip_validations=True</code> bypasses Pydantic validation with   <code>model_construct()</code>, but FastPG still fills <code>auto_now_add</code> and   auto-generated fields before writing.</li> </ul> <pre><code>from fastpg import OnConflict\n\n# Plain batch insert\nawait OrderItem.async_queryset.bulk_create(\n    [\n        {\n            \"order_id\": order.id,\n            \"product_id\": item[\"product_id\"],\n            \"quantity\": item[\"quantity\"],\n            \"unit_price\": item[\"unit_price\"],\n        }\n        for item in data[\"order_items\"]\n    ],\n    on_conflict=None,\n)\n\n# Ignore duplicates when an order item already exists\nawait OrderItem.async_queryset.bulk_create(\n    [\n        {\n            \"order_id\": order.id,\n            \"product_id\": item[\"product_id\"],\n            \"quantity\": item[\"quantity\"],\n            \"unit_price\": item[\"unit_price\"],\n        }\n        for item in data[\"order_items\"]\n    ],\n    on_conflict=OnConflict.DO_NOTHING,\n)\n\n# Upsert products by SKU, matching the shop demo API\nawait Product.async_queryset.bulk_create(\n    products_batch,\n    on_conflict=OnConflict.UPDATE,\n    conflict_target=[\"sku\"],\n    update_fields=[\"name\", \"category_id\", \"price\", \"stock_quantity\"],\n)\n</code></pre>"},{"location":"api/queries/#retrieval-helpers","title":"Retrieval helpers","text":"<p>origin/main</p> <ul> <li><code>select_related(*relation_names)</code></li> <li><code>filter_related(*conditions, **filters)</code></li> <li><code>prefetch_related(*prefetches)</code></li> </ul>"},{"location":"api/queries/#mutation-methods","title":"Mutation methods","text":"<ul> <li><code>create(on_conflict=None, conflict_target=None, update_fields=None, **kwargs)</code></li> <li><code>bulk_create(values, skip_validations=False, on_conflict=None, conflict_target=None, update_fields=None)</code></li> <li><code>get_or_create(defaults, **kwargs)</code></li> <li><code>update_or_create(defaults, **kwargs)</code></li> <li><code>update(**kwargs)</code></li> <li><code>delete()</code></li> </ul>"},{"location":"api/queries/#await-behavior","title":"Await behavior","text":"<p>Awaiting queryset executes SQL and returns based on action:</p> <ul> <li><code>get()</code> -&gt; one model/dict, or raises cardinality errors.</li> <li><code>filter()</code> / <code>all()</code> -&gt; list.</li> <li><code>count()</code> -&gt; integer.</li> <li><code>update()</code> / <code>delete()</code> -&gt; affected row count from write query.</li> </ul>"},{"location":"api/queries/#asyncrawqueryquery-usingnone","title":"<code>AsyncRawQuery(query, using=None)</code>","text":"<p>Raw SQL wrapper with FastPG error handling.</p>"},{"location":"api/queries/#methods","title":"Methods","text":"<ul> <li><code>fetch(values)</code> -&gt; list of dict records (read connection)</li> <li><code>execute(values)</code> -&gt; execute single write query</li> <li><code>execute_many(list_of_values)</code> -&gt; execute write query with many parameter sets</li> </ul>"},{"location":"api/queries/#asyncpaginatorpage_size-queryset-usingnone","title":"<code>AsyncPaginator(page_size, queryset, using=None)</code>","text":"<p>Paginates an <code>AsyncQuerySet</code> and returns <code>results</code> plus <code>results_paginator</code> metadata.</p>"},{"location":"api/queries/#rawqueryasyncpaginator","title":"<code>RawQueryAsyncPaginator(...)</code>","text":"<p>Paginates raw SQL with optional serializer and auto <code>LIMIT/OFFSET</code> handling.</p>"},{"location":"examples/advanced-usage/","title":"Advanced Usage","text":"<p>Examples below come directly from <code>test_project/app/api/endpoints/shop_api.py</code> patterns.</p>"},{"location":"examples/advanced-usage/#select-related-related-filtering","title":"Select related + related filtering","text":"<pre><code>from fastpg import OrderBy\n\nemployees = Employee.async_queryset.select_related(\"department\").all()\nemployees = employees.filter(salary__gte=50000)\nemployees = employees.filter_related(department__name=\"Engineering\")\nrows = await employees.order_by(salary=OrderBy.DESCENDING)\n</code></pre>"},{"location":"examples/advanced-usage/#prefetch-child-collections","title":"Prefetch child collections","text":"<pre><code>from fastpg import Prefetch, ReturnType\n\nrows = await (\n    Department.async_queryset\n    .prefetch_related(Prefetch(\"employees\", Employee.async_queryset.all()))\n    .all()\n    .return_as(ReturnType.DICT)\n)\n</code></pre>"},{"location":"examples/advanced-usage/#transaction-patterns","title":"Transaction patterns","text":"<pre><code>from fastpg import Transaction\n\n\nasync def create_department_with_employees(dept, employees):\n    async with Transaction.atomic():\n        department = await Department.async_queryset.create(**dept)\n        for emp in employees:\n            emp[\"department_id\"] = department.id\n            await Employee.async_queryset.create(**emp)\n    return department\n</code></pre>"},{"location":"examples/advanced-usage/#update-or-create","title":"Update-or-create","text":"<pre><code>product, created = await Product.async_queryset.update_or_create(\n    id=1,\n    sku=\"SKU-1\",\n    defaults={\"name\": \"Renamed\", \"price\": 12.0, \"stock_quantity\": 100},\n)\n</code></pre>"},{"location":"examples/advanced-usage/#queryset-pagination-for-api-responses","title":"Queryset pagination for API responses","text":"<pre><code>from fastpg import AsyncPaginator\n\npaginator = AsyncPaginator(\n    page_size=20,\n    queryset=Product.async_queryset.all(),\n)\n\npage = await paginator.get_page(1)\nreturn page\n</code></pre>"},{"location":"examples/basic-usage/","title":"Basic Usage","text":"<p>This example mirrors the <code>test_project</code> shop schema.</p> <pre><code>from fastpg import DatabaseModel, JsonData, OnConflict, OrderBy\n\n\nclass Category(DatabaseModel):\n    id: int | None = None\n    name: str\n    description: str\n\n    class Meta:\n        db_table = \"categories\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n\n\nclass Product(DatabaseModel):\n    id: int | None = None\n    sku: str\n    name: str\n    category_id: int | None = None\n    price: float\n    stock_quantity: int\n    properties: JsonData = {}\n\n    class Meta:\n        db_table = \"products\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n</code></pre> <p>Bulk upsert-like sync with <code>bulk_create</code>:</p> <pre><code>payload = [\n    {\n        \"sku\": \"SKU-1\",\n        \"name\": \"Cap\",\n        \"category_id\": 1,\n        \"price\": 15.0,\n        \"stock_quantity\": 10,\n        \"properties\": {\"color\": \"black\"},\n    },\n    {\n        \"sku\": \"SKU-2\",\n        \"name\": \"Tee\",\n        \"category_id\": 1,\n        \"price\": 20.0,\n        \"stock_quantity\": 8,\n        \"properties\": {\"size\": \"L\"},\n    },\n]\n\nawait Product.async_queryset.bulk_create(\n    values=payload,\n    on_conflict=OnConflict.UPDATE,\n    conflict_target=[\"sku\"],\n    update_fields=[\"name\", \"category_id\", \"price\", \"stock_quantity\", \"properties\"],\n)\n</code></pre> <p>Read and mutate:</p> <pre><code>products = await Product.async_queryset.all().order_by(id=OrderBy.ASCENDING)\n\nawait Product.async_queryset.filter(id=1).update(stock_quantity__add=5)\nawait Product.async_queryset.filter(id=1).update(properties__jsonb_set__color=\"blue\")\n</code></pre>"},{"location":"guides/models/","title":"Models","text":"<p><code>DatabaseModel</code> extends <code>pydantic.BaseModel</code> and is the base class for all FastPG models.</p>"},{"location":"guides/models/#model-definition","title":"Model definition","text":"<pre><code>from datetime import datetime\nfrom fastpg import DatabaseModel\n\n\nclass Department(DatabaseModel):\n    id: int | None = None\n    name: str\n    location: str\n    created_at: datetime | None = None\n\n    class Meta:\n        db_table = \"departments\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n        auto_now_add_fields = [\"created_at\"]\n</code></pre>"},{"location":"guides/models/#meta-options","title":"<code>Meta</code> options","text":"Option Required Meaning <code>db_table</code> Yes Backing PostgreSQL table name. <code>primary_key</code> No Primary key column name. <code>auto_generated_fields</code> No Fields removed before insert. <code>auto_now_add_fields</code> No Fields auto-populated during create/bulk_create if current value is <code>None</code>. <code>auto_now_fields</code> No Fields auto-populated before <code>save()</code> if current value is <code>None</code>. <code>relations</code> No Mapping of relation name to <code>Relation(...)</code>."},{"location":"guides/models/#instance-methods","title":"Instance methods","text":""},{"location":"guides/models/#savecolumns-liststr-none-none-bool","title":"<code>save(columns: list[str] | None = None) -&gt; bool</code>","text":"<p>Updates the current row using <code>Meta.primary_key</code> in the <code>WHERE</code> clause.</p> <ul> <li>Returns <code>True</code> if at least one row was updated.</li> <li>If <code>columns</code> is omitted, all model fields are included in <code>SET</code>.</li> </ul>"},{"location":"guides/models/#delete-bool","title":"<code>delete() -&gt; bool</code>","text":"<p>Deletes the current row by <code>Meta.primary_key</code>.</p> <ul> <li>Returns <code>True</code> if at least one row was deleted.</li> </ul>"},{"location":"guides/models/#json-fields-in-models","title":"JSON fields in models","text":"<p>Use <code>JsonData</code> for JSON/JSONB columns:</p> <pre><code>from fastpg import DatabaseModel, JsonData\n\n\nclass Product(DatabaseModel):\n    id: int | None = None\n    name: str\n    properties: JsonData = {}\n\n    class Meta:\n        db_table = \"products\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n</code></pre> <p><code>JsonData</code> serializes dict/list values for DB writes and preserves Python objects in normal model use.</p>"},{"location":"guides/pagination/","title":"Pagination","text":"<p>FastPG provides two paginator classes:</p> <ul> <li><code>AsyncPaginator</code> for <code>AsyncQuerySet</code></li> <li><code>RawQueryAsyncPaginator</code> for raw SQL</li> </ul> <p>Both return the same shape:</p> <pre><code>{\n  \"results\": [],\n  \"results_paginator\": {\n    \"number\": 1,\n    \"page_size\": 20,\n    \"has_next\": false,\n    \"has_previous\": false,\n    \"start_index\": 0,\n    \"end_index\": 0\n  }\n}\n</code></pre>"},{"location":"guides/pagination/#queryset-pagination","title":"Queryset pagination","text":"<pre><code>from fastpg import AsyncPaginator, OrderBy\n\nqueryset = Product.async_queryset.all().order_by(id=OrderBy.ASCENDING)\npaginator = AsyncPaginator(page_size=25, queryset=queryset)\n\npage1 = await paginator.get_page(page=1)\npage2 = await paginator.get_next_page()\n</code></pre> <p>Rules:</p> <ul> <li><code>page</code> must be <code>&gt;= 1</code>, else <code>InvalidPageError</code>.</li> <li><code>get_page()</code> applies <code>limit(page_size)</code> and computed <code>offset(...)</code>.</li> <li>Pass <code>using=\"conn_name\"</code> to execute on a specific read connection.</li> </ul>"},{"location":"guides/pagination/#raw-sql-pagination","title":"Raw SQL pagination","text":"<pre><code>from fastpg import RawQueryAsyncPaginator\n\npaginator = RawQueryAsyncPaginator(\n    page_size=50,\n    query=\"SELECT id, sku FROM products ORDER BY id\",\n    values={},\n    serializer=lambda rows: [{\"id\": r[\"id\"], \"sku\": r[\"sku\"]} for r in rows],\n)\n\npage = await paginator.get_page(page=1)\n</code></pre> <p><code>RawQueryAsyncPaginator</code> behavior:</p> <ul> <li><code>auto_offset_and_limit=True</code> (default): appends <code>LIMIT ... OFFSET ...</code> automatically.</li> <li><code>auto_offset_and_limit=False</code>: your query must include <code>{page_size}</code> and <code>{offset}</code> placeholders.</li> </ul>"},{"location":"guides/querysets/","title":"Querysets","text":"<p><code>AsyncQuerySet</code> builds SQL lazily and executes when awaited.</p>"},{"location":"guides/querysets/#execution-model","title":"Execution model","text":"<p>A queryset must end in one of these actions before awaiting:</p> <ul> <li><code>get(...)</code></li> <li><code>filter(...)</code></li> <li><code>all()</code></li> <li><code>count()</code></li> <li><code>update(...)</code></li> <li><code>delete()</code></li> </ul> <p>Otherwise, awaiting raises <code>MalformedQuerysetError</code>.</p>"},{"location":"guides/querysets/#read-methods","title":"Read methods","text":"<pre><code>user = await User.async_queryset.get(id=1)\nusers = await User.async_queryset.filter(name__icontains=\"ada\")\nall_users = await User.async_queryset.all()\ntotal = await User.async_queryset.filter(is_active=True).count()\n</code></pre> <p><code>get(...)</code> and <code>filter(...)</code> should be called with at least one condition (<code>Q(...)</code> or keyword filters).</p>"},{"location":"guides/querysets/#lookup-operators","title":"Lookup operators","text":"<p>Supported filter suffixes:</p> <ul> <li><code>__gt</code>, <code>__lt</code>, <code>__gte</code>, <code>__lte</code>, <code>__ne</code></li> <li><code>__in</code> (requires a non-empty list)</li> <li><code>__isnull</code></li> <li><code>__contains</code>, <code>__icontains</code></li> <li><code>__startswith</code>, <code>__istartswith</code></li> <li><code>__endswith</code>, <code>__iendswith</code></li> </ul>"},{"location":"guides/querysets/#combining-filters-with-q","title":"Combining filters with <code>Q</code>","text":"<pre><code>from fastpg import Q\n\nq = Q(city=\"Boston\") | Q(city=\"New York\")\nresults = await Customer.async_queryset.filter(q, is_active=True)\n</code></pre>"},{"location":"guides/querysets/#mutating-queries","title":"Mutating queries","text":"<ul> <li><code>create(**kwargs)</code> \u2013 insert a row and return the new model instance. FastPG   automatically removes auto-generated fields and populates <code>auto_now_add</code>   fields before writing.</li> <li><code>bulk_create(values, on_conflict, conflict_target=None, update_fields=None, skip_validations=False)</code> \u2013   insert many rows efficiently. Use <code>OnConflict.DO_NOTHING</code> to skip duplicate   rows or <code>OnConflict.UPDATE</code> to upsert them.</li> <li><code>get_or_create(defaults=None, **lookup)</code> \u2013 fetch a row matching the lookup or   insert a new one using <code>defaults</code>.</li> <li><code>update_or_create(defaults, **lookup)</code> \u2013 update a row with <code>defaults</code> when it   exists, or create it when it does not. Returns a tuple of   <code>(instance, created)</code> where <code>created</code> is a boolean.</li> <li><code>update(**kwargs)</code> \u2013 set or increment fields. Special suffixes include   <code>__add</code>, <code>__sub</code>, <code>__mul</code>, <code>__div</code>, <code>__jsonb</code>, <code>__jsonb_set__path[__type]</code>,   <code>__jsonb_remove</code> for JSON columns, and <code>__add_time</code>/<code>__sub_time</code> for   timestamp arithmetic.</li> <li><code>delete()</code> \u2013 remove rows that match the current filters.</li> </ul> <p>Both <code>update</code> and <code>delete</code> require a filter; FastPG raises <code>UnrestrictedUpdateError</code> or <code>UnrestrictedDeleteError</code> if you attempt to call them without a <code>WHERE</code> clause.</p>"},{"location":"guides/querysets/#bulk-create-and-upsert","title":"Bulk create and upsert","text":"<p><code>bulk_create()</code> is the write-side counterpart to queryset chaining: you hand it a list of dictionaries and FastPG validates each payload, applies <code>auto_now_add</code> / auto-generated field preprocessing, and sends the batch with <code>execute_many()</code>.</p> <pre><code>from fastpg import OnConflict\n\nawait Product.async_queryset.bulk_create(\n    products_batch,\n    on_conflict=OnConflict.UPDATE,\n    conflict_target=[\"sku\"],\n    update_fields=[\"name\", \"category_id\", \"price\", \"stock_quantity\"],\n)\n</code></pre> <p>The <code>create_products_in_bulk</code> endpoint in <code>test_project/app/api/endpoints/shop_api.py</code> uses that pattern to keep the catalogue in sync by treating <code>sku</code> as the unique key.</p> <p>Use <code>OnConflict.DO_NOTHING</code> when duplicates should be ignored rather than updated:</p> <pre><code>await OrderItem.async_queryset.bulk_create(\n    [\n        {\n            \"order_id\": order.id,\n            \"product_id\": item[\"product_id\"],\n            \"quantity\": item[\"quantity\"],\n            \"unit_price\": item[\"unit_price\"],\n        }\n        for item in data[\"order_items\"]\n    ],\n    on_conflict=OnConflict.DO_NOTHING,\n)\n</code></pre> <p>That matches the <code>create_order</code> endpoint, which inserts line items inside a transaction and skips duplicate <code>(order_id, product_id)</code> pairs.</p> <p>When duplicates should overwrite selected columns, pass <code>OnConflict.UPDATE</code> together with both <code>conflict_target</code> and <code>update_fields</code>:</p> <pre><code>await OrderItem.async_queryset.bulk_create(\n    [\n        {\n            \"order_id\": order.id,\n            \"product_id\": item[\"product_id\"],\n            \"quantity\": item[\"quantity\"],\n            \"unit_price\": item[\"unit_price\"],\n        }\n        for item in data[\"order_items\"]\n    ],\n    on_conflict=OnConflict.UPDATE,\n    conflict_target=[\"order_id\", \"product_id\"],\n    update_fields=[\"quantity\", \"unit_price\"],\n)\n</code></pre> <p>That is the same shape used by the <code>add_order_items</code> endpoint to upsert order lines by their composite key.</p> <ul> <li><code>values=[]</code> raises <code>NothingToCreateError</code>.</li> <li>Pass <code>on_conflict=None</code> explicitly if you want a plain batch insert with no   conflict clause.</li> <li><code>skip_validations=True</code> bypasses Pydantic validation, which is useful for   trusted pre-validated data paths but removes schema-level safety checks.</li> </ul>"},{"location":"guides/querysets/#update-suffix-examples","title":"Update suffix examples","text":"<p>Update suffixes let you express common mutations directly in SQL without round-tripping values through Python. A few practical examples from the shop demo API:</p> <pre><code>from fastpg import OrderBy, ReturnType\n\nrows = await (\n    Product.async_queryset\n    .columns(\"id\", \"name\", \"price\")\n    .filter(price__gte=100)\n    .order_by(price=OrderBy.DESCENDING)\n    .limit(20)\n    .offset(0)\n    .return_as(ReturnType.DICT)\n)\n</code></pre>"},{"location":"guides/querysets/#writes","title":"Writes","text":""},{"location":"guides/querysets/#createkwargs","title":"<code>create(**kwargs)</code>","text":"<p>Creates one row and returns a model instance.</p> <ul> <li><code>auto_now_add_fields</code> are set before insert when field value is <code>None</code>.</li> <li><code>auto_generated_fields</code> are excluded from insert values.</li> <li>Current implementation always performs plain <code>INSERT ... RETURNING</code>; passed <code>on_conflict</code> args are currently not applied.</li> </ul>"},{"location":"guides/querysets/#bulk_createvalues-skip_validationsfalse-on_conflictnone","title":"<code>bulk_create(values, skip_validations=False, on_conflict=None, ...)</code>","text":"<p>Creates many rows.</p> <ul> <li>Raises <code>NothingToCreateError</code> if <code>values</code> is empty.</li> <li>Supports:</li> <li><code>OnConflict.DO_NOTHING</code></li> <li><code>OnConflict.UPDATE</code> (requires <code>conflict_target</code> and <code>update_fields</code>)</li> </ul>"},{"location":"guides/querysets/#get_or_createdefaults-lookup-and-update_or_createdefaults-lookup","title":"<code>get_or_create(defaults, **lookup)</code> and <code>update_or_create(defaults, **lookup)</code>","text":"<p>Return tuple <code>(obj, created)</code>.</p>"},{"location":"guides/querysets/#update-operators","title":"<code>update(...)</code> operators","text":"<p>Standard set update:</p> <pre><code>await Product.async_queryset.filter(id=1).update(name=\"New Name\")\n</code></pre> <p>Special suffixes:</p> <ul> <li>Arithmetic: <code>__add</code>, <code>__sub</code>, <code>__mul</code>, <code>__div</code></li> <li>Time interval: <code>__add_time</code>, <code>__sub_time</code></li> <li>JSONB replace: <code>__jsonb</code></li> <li>JSONB key set: <code>__jsonb_set__key_name</code></li> <li>JSONB key remove: <code>__jsonb_remove</code></li> </ul> <p>Example:</p> <pre><code>await Product.async_queryset.filter(id=1).update(\n    stock_quantity__add=5,\n    properties__jsonb_set__color=\"blue\",\n)\n</code></pre>"},{"location":"guides/querysets/#delete","title":"<code>delete()</code>","text":"<p>Deletes rows matching current filters.</p> <pre><code>deleted_count = await Product.async_queryset.filter(id=1).delete()\n</code></pre>"},{"location":"guides/querysets/#important-safety-note","title":"Important safety note","text":"<p>Always call <code>filter(...)</code> before <code>update(...)</code> or <code>delete(...)</code>. That guarantees valid <code>WHERE</code> conditions in generated SQL.</p>"},{"location":"guides/querysets/#connection-selection","title":"Connection selection","text":"<p><code>using(conn_name)</code> overrides read connection for the query:</p> <pre><code>items = await Product.async_queryset.using(\"replica_1\").all()\n</code></pre>"},{"location":"guides/relationships/","title":"Relationships","text":"<p>FastPG relationships are explicit and lightweight.</p>"},{"location":"guides/relationships/#define-a-relation","title":"Define a relation","text":"<pre><code>from fastpg import DatabaseModel, Relation\n\n\nclass Department(DatabaseModel):\n    id: int\n    name: str\n\n    class Meta:\n        db_table = \"departments\"\n        primary_key = \"id\"\n\n\nclass Employee(DatabaseModel):\n    id: int\n    department_id: int | None = None\n    name: str\n\n    class Meta:\n        db_table = \"employees\"\n        primary_key = \"id\"\n        relations = {\n            \"department\": Relation(Department, foreign_field=\"department_id\"),\n        }\n</code></pre> <p><code>foreign_field</code> is the field on the base model table (<code>t.foreign_field = r.related_id_field</code>).</p>"},{"location":"guides/relationships/#select_related","title":"<code>select_related(...)</code>","text":"<p>Performs a left join and hydrates one related object per base row.</p> <pre><code>employee = await Employee.async_queryset.select_related(\"department\").get(id=1)\n</code></pre> <p>Current implementation uses the first relation name passed to <code>select_related(...)</code>.</p> <p>Use <code>filter_related(...)</code> to filter related table columns:</p> <pre><code>rows = await (\n    Employee.async_queryset\n    .select_related(\"department\")\n    .filter_related(department__name=\"Engineering\")\n    .all()\n)\n</code></pre>"},{"location":"guides/relationships/#prefetch_related","title":"<code>prefetch_related(...)</code>","text":"<p>Loads child collections in a second query and attaches them to each base object.</p> <pre><code>from fastpg import Prefetch, ReturnType\n\nrows = await (\n    Department.async_queryset\n    .prefetch_related(Prefetch(\"employees\", Employee.async_queryset.all()))\n    .all()\n    .return_as(ReturnType.DICT)\n)\n</code></pre> <p>Requirements for prefetch:</p> <ul> <li>Prefetch queryset model must define a relation that points back to the base model.</li> <li>Otherwise FastPG raises <code>InvalidPrefetchError</code>.</li> </ul>"},{"location":"guides/relationships/#choosing-strategy","title":"Choosing strategy","text":"<ul> <li>Use <code>select_related</code> for one-to-one or many-to-one style joins.</li> <li>Use <code>prefetch_related</code> for one-to-many collections.</li> </ul>"},{"location":"guides/transactions/","title":"Transactions","text":"<p>FastPG exposes transaction helpers through <code>Transaction</code>.</p>"},{"location":"guides/transactions/#context-manager-recommended","title":"Context manager (recommended)","text":"<pre><code>from fastpg import Transaction\n\n\nasync def create_department_and_employees(dept_data, employees_data):\n    async with Transaction.atomic():\n        department = await Department.async_queryset.create(**dept_data)\n        for emp in employees_data:\n            emp[\"department_id\"] = department.id\n            await Employee.async_queryset.create(**emp)\n    return department\n</code></pre>"},{"location":"guides/transactions/#manual-flow","title":"Manual flow","text":"<pre><code>transaction = await Transaction.start()\ntry:\n    await Order.async_queryset.create(customer_id=1, total_amount=99.5, status=\"PENDING\", order_date=\"2025-01-01\")\nexcept Exception:\n    await transaction.rollback()\n    raise\nelse:\n    await transaction.commit()\n</code></pre>"},{"location":"guides/transactions/#decorator-style","title":"Decorator style","text":"<pre><code>from fastpg import Transaction\n\n\n@Transaction.decorator()\nasync def create_coupon(payload):\n    return await Coupon.async_queryset.create(**payload)\n</code></pre>"},{"location":"guides/transactions/#notes","title":"Notes","text":"<ul> <li>Transactions use the configured write connection.</li> <li>Open connections first (<code>connect_all()</code>) before starting transactions.</li> </ul>"},{"location":"reference/constants/","title":"Constants And Helpers","text":""},{"location":"reference/constants/#enumsconstants","title":"Enums/constants","text":""},{"location":"reference/constants/#connectiontype","title":"<code>ConnectionType</code>","text":"<ul> <li><code>ConnectionType.READ</code></li> <li><code>ConnectionType.WRITE</code></li> </ul>"},{"location":"reference/constants/#orderby","title":"<code>OrderBy</code>","text":"<ul> <li><code>OrderBy.ASCENDING</code> (<code>\"ASC\"</code>)</li> <li><code>OrderBy.DESCENDING</code> (<code>\"DESC\"</code>)</li> </ul>"},{"location":"reference/constants/#onconflict","title":"<code>OnConflict</code>","text":"<ul> <li><code>OnConflict.DO_NOTHING</code></li> <li><code>OnConflict.UPDATE</code></li> </ul>"},{"location":"reference/constants/#returntype","title":"<code>ReturnType</code>","text":"<ul> <li><code>ReturnType.MODEL_INSTANCE</code></li> <li><code>ReturnType.DICT</code></li> </ul>"},{"location":"reference/constants/#relationship-helpers","title":"Relationship helpers","text":""},{"location":"reference/constants/#relationrelated_model-foreign_field-related_namenone","title":"<code>Relation(related_model, foreign_field, related_name=None)</code>","text":"<p>Defines join metadata for <code>select_related</code>.</p>"},{"location":"reference/constants/#prefetchdataset_name-queryset","title":"<code>Prefetch(dataset_name, queryset)</code>","text":"<p>Defines prefetch metadata for <code>prefetch_related</code>.</p>"},{"location":"reference/constants/#query-helper","title":"Query helper","text":""},{"location":"reference/constants/#q","title":"<code>Q(...)</code>","text":"<p>Builds reusable SQL where fragments with supported lookup operators and supports <code>&amp;</code> / <code>|</code> composition.</p>"},{"location":"reference/errors/","title":"Errors","text":"<p>FastPG defines explicit exception classes in <code>fastpg.errors</code>.</p>"},{"location":"reference/errors/#connection-and-configuration","title":"Connection and configuration","text":"<ul> <li><code>ReadConnectionNotAvailableError</code></li> <li><code>MultipleWriteConnectionsError</code></li> <li><code>InvalidConnectionNameError</code></li> <li><code>FastPGInstanceNotConfiguredError</code></li> <li><code>MalformedMetaError</code></li> <li><code>MalformedQuerysetError</code></li> </ul>"},{"location":"reference/errors/#query-construction-and-operators","title":"Query construction and operators","text":"<ul> <li><code>InvalidINClauseValueError</code></li> <li><code>UnsupportedOperatorError</code></li> <li><code>InvalidRelatedFieldError</code></li> <li><code>InvalidPrefetchError</code></li> </ul>"},{"location":"reference/errors/#result-shape-and-cardinality","title":"Result shape and cardinality","text":"<ul> <li><code>DoesNotExist</code></li> <li><code>MultipleRecordsFound</code></li> </ul>"},{"location":"reference/errors/#mutation-safety","title":"Mutation safety","text":"<ul> <li><code>NothingToCreateError</code></li> <li><code>UnrestrictedUpdateError</code></li> <li><code>UnrestrictedDeleteError</code></li> </ul>"},{"location":"reference/errors/#pagination","title":"Pagination","text":"<ul> <li><code>InvalidPageError</code></li> </ul>"},{"location":"reference/errors/#database-execution","title":"Database execution","text":"<ul> <li><code>DatabaseError</code></li> <li><code>DuplicateKeyDatabaseError</code></li> </ul> <p><code>DatabaseError</code> includes SQLSTATE and driver exception class name when available.</p>"},{"location":"reference/json-fields/","title":"JSON Fields","text":"<p>FastPG includes JSON helpers in <code>fastpg.fields</code>.</p>"},{"location":"reference/json-fields/#jsondata","title":"<code>JsonData</code>","text":"<p><code>JsonData</code> is an annotated Pydantic type for JSON-compatible values.</p> <pre><code>from fastpg import DatabaseModel, JsonData\n\n\nclass Product(DatabaseModel):\n    id: int | None = None\n    properties: JsonData = {}\n\n    class Meta:\n        db_table = \"products\"\n        primary_key = \"id\"\n        auto_generated_fields = [\"id\"]\n</code></pre> <p>Behavior:</p> <ul> <li>For DB writes (<code>model_dump(context={\"db_write\": True})</code>), dict/list values are serialized to JSON.</li> <li>For normal serialization, Python objects are preserved.</li> </ul>"},{"location":"reference/json-fields/#helper-functions","title":"Helper functions","text":"<ul> <li><code>json_str_to_dict(value)</code>: if <code>value</code> is a JSON string, parse it.</li> <li><code>validate_json_data(data)</code>: serialize dict/list values for validation flow.</li> <li><code>serialize_json_data(data, info)</code>: serializer used by <code>JsonData</code>.</li> </ul>"},{"location":"reference/json-fields/#json-update-operators-in-queryset-update","title":"JSON update operators in queryset <code>update(...)</code>","text":"<ul> <li><code>field__jsonb={...}</code>: replace JSONB column.</li> <li><code>field__jsonb_set__key=value</code>: set one JSON key.</li> <li><code>field__jsonb_remove=\"key\"</code>: remove one JSON key.</li> </ul> <p>Example:</p> <pre><code>await Product.async_queryset.filter(id=1).update(\n    properties__jsonb_set__color=\"blue\",\n)\n</code></pre>"},{"location":"reference/preprocessors/","title":"Preprocessors","text":"<p>FastPG applies lightweight preprocessors around create/save operations.</p>"},{"location":"reference/preprocessors/#precreateprocessors","title":"<code>PreCreateProcessors</code>","text":""},{"location":"reference/preprocessors/#model_obj_populate_auto_now_add_fieldsmodel_obj","title":"<code>model_obj_populate_auto_now_add_fields(model_obj)</code>","text":"<ul> <li>Reads <code>model_obj.Meta.auto_now_add_fields</code>.</li> <li>For each listed field: if current value is <code>None</code>, sets <code>datetime.now(fastpg.TZ)</code>.</li> </ul>"},{"location":"reference/preprocessors/#model_dict_populate_auto_generated_fieldsmodel_dict-model_cls","title":"<code>model_dict_populate_auto_generated_fields(model_dict, model_cls)</code>","text":"<ul> <li>Reads <code>model_cls.Meta.auto_generated_fields</code>.</li> <li>Deletes those keys from insert payload before SQL execution.</li> </ul>"},{"location":"reference/preprocessors/#presaveprocessors","title":"<code>PreSaveProcessors</code>","text":""},{"location":"reference/preprocessors/#model_obj_populate_auto_now_fieldsmodel_obj","title":"<code>model_obj_populate_auto_now_fields(model_obj)</code>","text":"<ul> <li>Reads <code>model_obj.Meta.auto_now_fields</code>.</li> <li>For each listed field: if current value is <code>None</code>, sets <code>datetime.now(fastpg.TZ)</code>.</li> </ul>"},{"location":"reference/preprocessors/#where-preprocessors-are-used","title":"Where preprocessors are used","text":"<ul> <li><code>AsyncQuerySet.create(...)</code></li> <li><code>AsyncQuerySet.bulk_create(...)</code></li> <li><code>DatabaseModel.save(...)</code></li> </ul> <p>These helpers fail open when optional <code>Meta</code> lists are not defined.</p>"},{"location":"reference/settings/","title":"Settings And Configuration","text":""},{"location":"reference/settings/#create_fastpg","title":"<code>create_fastpg(...)</code>","text":"<p>Primary entry point:</p> <pre><code>create_fastpg(\n    name=\"default\",\n    databases={...},\n    tz_name=\"UTC\",\n    query_logger={\"LOG_QUERIES\": True, \"TITLE\": \"MY_APP\"},\n    db_conn_manager_class=None,\n)\n</code></pre>"},{"location":"reference/settings/#databases-config-format","title":"Databases config format","text":"<p>Each connection entry contains:</p> <ul> <li><code>TYPE</code>: <code>ConnectionType.READ</code> or <code>ConnectionType.WRITE</code></li> <li><code>USER</code></li> <li><code>PASSWORD</code></li> <li><code>DB</code></li> <li><code>HOST</code></li> <li><code>PORT</code></li> </ul> <p>Example:</p> <pre><code>from fastpg import ConnectionType\n\ndatabases = {\n    \"default\": {\n        \"TYPE\": ConnectionType.WRITE,\n        \"USER\": \"postgres\",\n        \"PASSWORD\": \"postgres\",\n        \"DB\": \"app_db\",\n        \"HOST\": \"127.0.0.1\",\n        \"PORT\": 5432,\n    },\n    \"replica_1\": {\n        \"TYPE\": ConnectionType.READ,\n        \"USER\": \"postgres\",\n        \"PASSWORD\": \"postgres\",\n        \"DB\": \"app_db\",\n        \"HOST\": \"127.0.0.1\",\n        \"PORT\": 5433,\n    },\n}\n</code></pre>"},{"location":"reference/settings/#timezone","title":"Timezone","text":"<ul> <li><code>tz_name</code> controls timezone for automatic timestamp processors.</li> <li>Invalid timezone names fallback to <code>UTC</code>.</li> </ul>"},{"location":"reference/settings/#query-logging","title":"Query logging","text":"<p>Enable DB timing logs:</p> <pre><code>query_logger={\n    \"LOG_QUERIES\": True,\n    \"TITLE\": \"SHOP_API\",\n}\n</code></pre> <p>When enabled, logs are emitted from <code>fastpg.utils</code> and bucketed by elapsed time.</p>"},{"location":"reference/settings/#registry-helpers","title":"Registry helpers","text":"<p>FastPG supports multiple named instances:</p> <ul> <li><code>register_fastpg(name, instance)</code></li> <li><code>create_fastpg(name=...)</code></li> <li><code>get_fastpg(name=None)</code></li> <li><code>set_current_fastpg(name)</code></li> </ul> <p><code>get_fastpg()</code> without <code>name</code> returns the current context-bound instance.</p>"},{"location":"reference/settings/#connection-manager-api","title":"Connection manager API","text":"<p><code>DBConnectionManager</code> methods:</p> <ul> <li><code>connect_all()</code></li> <li><code>close_all()</code></li> <li><code>get_db_conn(conn_name)</code></li> <li><code>db_for_read()</code></li> <li><code>db_for_write()</code></li> </ul> <p>Validation behavior:</p> <ul> <li>Raises <code>ReadConnectionNotAvailableError</code> if no read connection exists.</li> <li>Raises <code>MultipleWriteConnectionsError</code> if more than one write connection is configured.</li> </ul>"}]}